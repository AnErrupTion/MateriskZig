struct VgaEntry {
    character: u8,
    color: u8,
};

func main(): void {
    var mut buffer: *VgaEntry = cast: *VgaEntry 0xB8000;
    var entry: VgaEntry = init VgaEntry {
        character = 65,
        color = 15,
    };

    *buffer = entry;
};
